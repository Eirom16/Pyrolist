[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "pyrolist-native-rs"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
module-name = "pyrolist.native_rs"
